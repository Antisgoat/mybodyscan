export const MBS_FLAGS = {
  ENABLE_BRAND_NAVBAR: true,
  ENABLE_ONBOARDING_WIZARD: true,
  ENABLE_NAV_GUARD: true,
  ENABLE_TIPS_CARD: true,
  ENABLE_ERROR_TOAST: true,
  ENABLE_FIREBASE_INIT_FALLBACK: true, // only used if no existing firebase client found
} as const;