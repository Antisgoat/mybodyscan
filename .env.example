# Copy these into your environment (.env.development / .env.production and Lovable env UI)
# Auth providers default to enabled when unset; override to false to hide a provider.
VITE_ENABLE_GOOGLE=true
VITE_ENABLE_APPLE=true
VITE_ENABLE_EMAIL=true
VITE_ENABLE_DEMO=true

# Optional overrides for firebase (if you have private keys set in CI, leave these blank locally)
# VITE_FIREBASE_API_KEY=
# VITE_FIREBASE_AUTH_DOMAIN=
# VITE_FIREBASE_PROJECT_ID=
# VITE_FIREBASE_STORAGE_BUCKET=
# VITE_FIREBASE_MESSAGING_SENDER_ID=
# VITE_FIREBASE_APP_ID=
# VITE_FIREBASE_MEASUREMENT_ID=

# Other optional flags/features
VITE_ENABLE_PUBLIC_MARKETING_PAGE=true

# Notes:
# - Fill the real values from Firebase Console → Project settings → Your apps (Web) if you want to override the fallback config.
# - In Lovable, add these in Project Settings → Environment (prefix must be VITE_).
