{
  "type": "module",
  "engines": {
    "node": "20"
  },
  "main": "lib/index.js",
  "scripts": {
    "clean": "node --input-type=module -e \"import { rmSync } from 'node:fs'; rmSync('lib', { recursive: true, force: true });\"",
    "build": "npm run clean && tsc --project ./tsconfig.json && node --input-type=module -e \"import { existsSync } from 'node:fs'; if (!existsSync('lib/index.js')) { console.error('Missing lib/index.js after build'); process.exit(2); }\"",
    "build:resilient": "npm run build || (npm cache clean --force && rm -rf node_modules && npm install --prefer-online && npm run build)",
    "ci-or-install": "npm ci || npm install --prefer-online --no-audit --no-fund",
    "typecheck": "tsc --noEmit -p ./tsconfig.json",
    "list": "tsc -p ./tsconfig.json --listFiles",
    "lint": "echo \"skip\"",
    "test": "npm run build && node --test ./test/**/*.test.js",
    "deploy:functions": "npm run build && firebase deploy --only functions --project mybodyscan-f3daf",
    "set:staff": "ts-node scripts/setStaffClaim.ts"
  },
  "dependencies": {
    "@google-cloud/storage": "^7.11.0",
    "@types/node": "^20.11.0",
    "express": "^4.18.2",
    "firebase-admin": "^12.6.0",
    "firebase-functions": "^5.1.0",
    "openai": "file:vendor/openai",
    "express-rate-limit": "file:vendor/express-rate-limit",
    "stripe": "^16.0.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "rimraf": "^5.0.10",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.0"
  }
}
