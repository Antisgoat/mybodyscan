# Changelog

## Unreleased

- Added demo entry and exit helpers with a CTA on the auth page plus a header leave-demo option.
- Enabled Stripe plan checkout through the new billing endpoint and refreshed plans page messaging.
- Rebuilt the coach chat endpoint to return OpenAI replies while persisting history and wired the UI through the new client helper.
- Hardened demo read-only protections across purchase and coach interactions with shared guard messaging.
